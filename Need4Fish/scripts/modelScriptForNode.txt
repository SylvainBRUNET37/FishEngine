// ONE

import bpy
import json

obj = bpy.context.active_object

data = {
    "components": {
        "rigidBody": {
            "type": "boxShape"
        },
        "eatable": {
            "mass": 100.0,
            "isApex": False
        },
        "controllable": {
            "maxSpeed": 200.0
        }
    }
}

obj["components"] = json.dumps(data["components"])

// ALL

import bpy
import json

data = {
    "components": {
        "rigidBody": {
            "type": "meshShape"
        }
    }
}

components_json = json.dumps(data["components"])

# Apply on every selected components
for obj in bpy.context.selected_objects:
    obj["components"] = components_json

